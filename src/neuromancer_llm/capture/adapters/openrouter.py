"""OpenRouter adapter — require_parameters=true + provider pinning + the second GET /generation payload
stored as a LINKED capture row (lineage_edges edge_kind='linked_call', A7). STAGE 2.
"""

from __future__ import annotations
