"""vllm-lens adapter — fast resid-only capture + steering at scale (UK AISI). STAGE 2.

The vllm-lens<->vllm tested version pair is an open [research] item resolved at bring-up, not from memory.
"""

from __future__ import annotations
