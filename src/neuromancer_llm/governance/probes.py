"""Scheduled operator probes (systemd timer targets): restore-drill, desktop health agent (ADR-0015),
mirror audit — report to PG + blob, freshness surfaced in the preflight banner. STAGE 2.
"""

from __future__ import annotations
