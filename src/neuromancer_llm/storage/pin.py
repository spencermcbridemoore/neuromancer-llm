"""Pin = promote-at-publication + manual `neuro pin` (ADR-0034). Budget stays lazy by default; a
desktop failure before pin falls back to the recorded recompute recipe. STAGE 2.
"""

from __future__ import annotations
