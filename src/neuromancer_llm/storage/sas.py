"""User-delegation SAS lifecycle — minted with a stated TTL, expiry surfaced in the daily probe AND the
generated views-file header (ADR-0013). The SAS is a secret. STAGE 2.
"""

from __future__ import annotations
