# Normalize line endings to LF in the working tree on every platform so the byte-stable generated-docs
# gate (`neuro docs build --check`) holds — a CRLF checkout would otherwise diff against LF-generated docs.
* text=auto eol=lf
*.whl binary
